{
	"role" : {
	 "name" : "default_access_role",
	 "schema_roles" : [
		{
			"names" : [ "DSO_ROLLBACK_CHANGES_ROLE", "DSO_ACTIVATE_CHANGES_ROLE" ]
		}
	 ],
	 "schema_privileges" : [
		{
			"privileges" : [ "SELECT", "INSERT", "UPDATE", "DELETE", "EXECUTE", "CREATE TEMPORARY TABLE", "SELECT CDS METADATA" ]
		}
		]
	}
}